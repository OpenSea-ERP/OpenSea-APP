/**
 * /hr/punch — manager dashboard layout (Phase 7 / Plan 07-06).
 *
 * Currently a passthrough; reserved for future shared breadcrumb / context
 * providers (e.g. tab-strip across dashboard/approvals/health/missing).
 */

import type { ReactNode } from 'react';

export default function PunchLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
